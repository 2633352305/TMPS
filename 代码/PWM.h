/*************************************************************
                      PWM头文件
 
实现功能：PWM信号的生成与控制

功能说明：使用定时器1生成PWM信号控制电机转速
***************************************************************/
#ifndef _PWM_H_
#define _PWM_H_
#include <reg52.H>	 
#include <intrins.H> 
 
#define  uchar unsigned char
#define  uint unsigned int	

/*****************PWM引脚定义*******************/
sbit PWM_OUT = P1^5;    // PWM输出引脚，连接到L298N驱动板

/*****************PWM变量定义*******************/
uchar PWM_duty = 0;     // PWM占空比值(0-100)
uchar PWM_count = 0;    // PWM计数值

/*****************PWM函数定义*******************/
void PWM_init(void);                // PWM初始化

/********************************************************
函数名称:void PWM_init(void)
函数功能:PWM初始化函数
参数说明:无
********************************************************/
// 统一定时器初始化逻辑
void PWM_init(void);

// 注意：定时器中断服务函数已移至PWM.c文件中实现
// 避免与PWM.c中的Timer1中断函数冲突

#endif